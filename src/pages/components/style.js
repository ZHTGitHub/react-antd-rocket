import styled from 'styled-components'

export const MainWrapper = styled.div`
  display: flex;
  flex-flow: row wrap;
  
  .main-menu {

  }

  .main-container {
    width: calc(100% - 256px);
    padding: 16px;
  }
`